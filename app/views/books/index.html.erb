 <% @books.each do |book| %>
<main>
    <div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3">
      <h2>User info</h2>
      <%= image_tag @user.get_profile_image %>
  <table class="table">
    <tbody>
    <tr></tr>
    <tr>
    <th>name</th>
	  <th><%= @user.name %></th>
  	</tr>
  	<tr>
	  <th>introduction</th>
	  <th><%= @user.introduction %></th>
    </tr>
  </tbody>
	</table>
    <div class="row">
    <%= link_to edit_user_path,class: "btn btn-outline-secondary btn-block" do %>
    <i class="fa-solid fa-user-gear"></i>
    <% end %>
    </div>
    <h2 class="mt-3">New book</h2>
    <%= form_with model: @book, url: '/books', method: :post do  |f| %>
     <div><%= f.label :title %></div>
     <div><%= f.text_field :title %></div>
    <br>
      <div><%= f.label :Opinion %></div>
      <div><%= f.text_area :Opinion %></div>

    <input type="submit" name="commit" value="Create Book" class="btn btn-success" data-disable-with="Create Book">
    <% end %>


<h2>Book detail</h2>
<table class="table table-hover table-inverse">
  <thead>
    <tr>
      <th><% if @book.profile_image.attached? %></th>
      <link_to "",>
      <th><%= f.label :title %></th>
      <th><%= f.label :opinion %></th>
      <th><%= link_to "Edit", edit_book_path(book.id),class: "btn btn-sm btn-success"do %></th>
      <th> <% if @book.user == current_user %>
      <%= link_to "Destroy", destroy_book_path(book.id),class: "btn btn-sm btn-danger" do  method: :delete, data: { confirm: "本当に削除しますか？"} %>
      <% end %>
      </th>
    </tr>
  </thead>
</table>

 <%= link_to 'New book', new_book_path %>
 </div>
 </div>
</main>
<% end %>

